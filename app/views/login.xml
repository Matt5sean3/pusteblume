<Alloy>
	<Model src="Session" /><!-- User Data Singleton -->
    <Window id="window" zIndex="100">
        <View id="view_login">
            <Label id="lbl_login" textid="loginTitle"></Label>
            <TextField id="username" hintText="Username@pod"></TextField>
            <TextField id="password" hintText="Password"></TextField>
            <Button id="btn_login" titleid="login"></Button>
            <!--<Button id="btn_register" titleid="register"></Button>-->
        </View>
        <!-- 
        	 Investigate whether there's a more android specific waiting, 
             I seem to remember one existing
        -->
        <ActivityIndicator id="waiting"></ActivityIndicator>
    </Window>
</Alloy>